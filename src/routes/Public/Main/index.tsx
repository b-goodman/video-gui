import React, {FunctionComponent, } from "react";
import "./index.scss";

interface Props {}

const Main: FunctionComponent<Props> = (props) => {

    return (
        <div>
            Main
        </div>
    )
}

export default Main;
